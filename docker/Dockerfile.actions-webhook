FROM golang:1.26.2-alpine AS build
WORKDIR /src
COPY cmd/actions-webhook/go.mod ./
COPY cmd/actions-webhook/*.go ./
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /webhook .

FROM alpine:3.21
COPY --from=build /webhook /webhook
ENTRYPOINT ["/webhook"]
