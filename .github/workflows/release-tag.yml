name: Release tag

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      # Comma-separated list of artifact names to download
      artifacts:
        required: false
        type: string
        description: "Comma-separated artifact names (e.g., 'go-api,events-api,js-api')"
      # Comma-separated list of directories for artifacts
      artifact_dirs:
        required: false
        type: string
        description: "Comma-separated target directories matching artifacts (e.g., 'api,events,js-client')"
      # Comma-separated list of directories to cleanup
      cleanup_dirs:
        required: false
        type: string
        description: "Comma-separated directories to remove (e.g., 'openapi,asyncapi,avro')"
      # Whether to create clean release branch with only artifacts
      clean_release:
        required: false
        type: boolean
        default: true
        description: "Create orphan release branch with only artifacts (true) or keep all files (false)"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Debug Inputs
        run: |
          echo "version: ${{ inputs.version }}"
          echo "artifacts: ${{ inputs.artifacts }}"
          echo "artifact_dirs: ${{ inputs.artifact_dirs }}"
          echo "cleanup_dirs: ${{ inputs.cleanup_dirs }}"
          echo "clean_release: ${{ inputs.clean_release }}"

      - name: Check if tag already exists
        run: |
          if git rev-parse "refs/tags/${{ inputs.version }}" >/dev/null 2>&1; then
            echo "❌ Tag ${{ inputs.version }} already exists"
            exit 1
          fi

      - name: Configure Git
        if: ${{ inputs.artifacts != '' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Cleanup unnecessary directories
        if: ${{ inputs.cleanup_dirs != '' }}
        run: |
          echo "🧹 Cleaning up unnecessary directories before downloading artifacts..."
          
          IFS=',' read -ra CLEANUP_DIRS <<< "${{ inputs.cleanup_dirs }}"
          
          for dir in "${CLEANUP_DIRS[@]}"; do
            # Trim whitespace
            dir=$(echo "$dir" | xargs)
            
            if [ -d "$dir" ]; then
              rm -rf "$dir" && echo "✓ Removed $dir" || echo "❌ Failed to remove $dir"
            else
              echo "  $dir not found (skipping)"
            fi
          done
          
          echo "✅ Cleanup complete"

      - name: Download artifacts
        if: ${{ inputs.artifacts != '' }}
        run: |
          IFS=',' read -ra ARTIFACTS <<< "${{ inputs.artifacts }}"
          IFS=',' read -ra DIRS <<< "${{ inputs.artifact_dirs }}"
          
          for i in "${!ARTIFACTS[@]}"; do
            artifact=$(echo "${ARTIFACTS[$i]}" | xargs)
            target_dir=$(echo "${DIRS[$i]}" | xargs)
            
            echo "→ Downloading artifact: $artifact to $target_dir"
            
            # Download artifact directly to target directory
            # gh run download extracts artifact contents directly (no subdirectory with artifact name)
            if gh run download ${{ github.run_id }} --name "$artifact" --dir "$target_dir"; then
              echo "✓ Successfully downloaded $artifact to $target_dir"
              echo "Contents of $target_dir:"
              ls -la "$target_dir" || echo "Directory is empty or does not exist"
            else
              echo "❌ Failed to download $artifact"
              exit 1
            fi
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Debug directory structure
        if: ${{ inputs.artifacts != '' }}
        run: |
          echo "📁 Current directory structure:"
          ls -la
          echo ""
          echo "📁 All downloaded artifacts:"
          find . -type f -name "*.go" -o -name "*.yaml" -o -name "*.yml" | head -20

      - name: Commit artifacts to `release` branch
        if: ${{ inputs.artifacts != '' }}
        run: |
          if [ "${{ inputs.clean_release }}" = "true" ]; then
            echo "📦 Creating clean release branch (only artifacts)"
            # Create orphan release branch (no history, clean slate)
            git checkout --orphan release
            # Remove all files from staging
            git rm -rf --cached . || true
          else
            echo "� Creating release branch (keeping all files)"
            # Create regular branch from current state
            git checkout -B release
          fi
          
          echo "📁 Files in working directory:"
          ls -la
          
          # Add only artifact directories
          IFS=',' read -ra DIRS <<< "${{ inputs.artifact_dirs }}"
          for dir in "${DIRS[@]}"; do
            dir=$(echo "$dir" | xargs)
            if [ -d "$dir" ] && [ -n "$(ls -A $dir 2>/dev/null)" ]; then
              echo "→ Adding $dir to release branch"
              git add -f "$dir"
            else
              echo "⚠️  Directory $dir is empty or doesn't exist"
            fi
          done
          
          echo ""
          echo "📁 Staged files:"
          git status
          
          if git diff --cached --quiet; then
            echo "⚠️  No changes to commit"
          else
            git commit -m "Add API artifacts to release ${{ inputs.version }}"
            git push -u origin release --force
            echo "✓ Pushed artifacts to release branch"
          fi

      - name: Tag release
        run: |
          if [ -z "${{ inputs.version }}" ]; then
            echo "❌ No version provided"
            exit 1
          fi

          git tag ${{ inputs.version }}
          git push origin ${{ inputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: Release ${{ inputs.version }}
          body: |
            API generated automatically for version ${{ inputs.version }}.
